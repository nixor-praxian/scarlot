# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
*.swp
*.swo
*~
.idea/
.vscode/
.obsidian/

# Node (for POC)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Binary exports (use markdown source of truth)
*.docx

# Financial model notes (xlsx is tracked, but keep markdown working notes local)
docs/business/financial-model/

# Claude Code
.claude/

# Raw sync recordings and transcripts (kept locally; structured records are committed)
docs/discovery/syncs/*/

# Harness session record (not Scarlot-specific; reusable pattern lives in docs/meta/)
docs/discovery/syncs/SCARLOT_SYNC_20260430_hackathon-day1-noon-harness.md

# NanoClaw legacy submodule remnants (regenerated by stale launchd job; safe to delete locally)
engine/
